# NER-SHIELD backend dependencies.
# NOTE: pins are minimums observed working on Python 3.14 (Windows); loosen rather
# than pin exact old versions — pydantic-core/asyncpg have no cp314 wheels for them.
fastapi>=0.115
uvicorn[standard]>=0.32
SQLAlchemy[asyncio]>=2.0.40
asyncpg>=0.30
pydantic>=2.9
pydantic-settings>=2.6
email-validator>=2.2
httpx>=0.27
PyJWT[crypto]>=2.9
python-multipart>=0.0.17

# dev/test
pytest>=8.3
pytest-asyncio>=0.24
asgi-lifespan>=2.1
